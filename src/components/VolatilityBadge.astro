---
type Volatility = 'HIGH' | 'MEDIUM' | 'LOW';

interface Props {
  level: Volatility;
}

const { level } = Astro.props;
---

<span class={`volatility-badge volatility--${level.toLowerCase()}`}>{level}</span>
