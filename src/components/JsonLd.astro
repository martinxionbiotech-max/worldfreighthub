---
interface Props {
  type: string;
  data: Record<string, any>;
}

const { type, data } = Astro.props;
const payload = { '@context': 'https://schema.org', '@type': type, ...data };
---

<script is:inline type="application/ld+json" set:html={JSON.stringify(payload)}></script>
